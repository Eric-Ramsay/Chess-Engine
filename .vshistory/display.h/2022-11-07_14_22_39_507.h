#pragma once
#include "structs.h"

std::string moveToString() {

}

//Takes a string like e2-e4 and a list of possible moves and returns the index of the correct move
short findMove(std::vector<Move> moves) {

}

//Takes a vector board as an argument, and returns a stylized string showing the board
std::string displayBoard(std::vector<Piece> board) {

	return "";
}